// components/exams/PurpleBackground.jsx
import React from "react";

// This component has been modified to not display any background
// We kept the component to avoid breaking references to it
const PurpleBackground = () => {
  // Return an empty div without any visual elements
  return null;
};

export default PurpleBackground;
